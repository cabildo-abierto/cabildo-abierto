import RepeatIcon from '@mui/icons-material/Repeat';
import {FontSizeType} from "./icon-utils";

export const RepostIcon = ({fontSize, color="inherit"}: {fontSize?: FontSizeType, color?: string}) => {
    return <RepeatIcon fontSize={fontSize} color={"inherit"}/>
}