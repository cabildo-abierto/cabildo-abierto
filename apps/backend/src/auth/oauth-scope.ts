/** Space-separated OAuth scopes for AT Protocol login (must match client-metadata / authorize requests). */
export const ATPROTO_OAUTH_SCOPE =
    "atproto transition:generic transition:email transition:chat.bsky identity:handle"
